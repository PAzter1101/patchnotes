apiVersion: v1
kind: Secret
metadata:
  name: {{ include "patchnotes.fullname" . }}-secrets
  labels:
    {{- include "patchnotes.labels" . | nindent 4 }}
type: Opaque
stringData:
  LLM_API_KEY: {{ .Values.secrets.llmApiKey | quote }}
  GIT_TOKEN: {{ .Values.secrets.gitToken | quote }}
